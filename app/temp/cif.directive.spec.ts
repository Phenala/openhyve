import { CifDirective } from './cif.directive';

describe('CifDirective', () => {
  it('should create an instance', () => {
    const directive = new CifDirective();
    expect(directive).toBeTruthy();
  });
});
